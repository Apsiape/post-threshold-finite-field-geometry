## Withdrawn and corrected claims

This document preserves the correction history needed for a responsible public release.

## Withdrawn

1. **Earlier full-ambient claim at `(p, k) = (3, 5)`.**
   - Earlier statement: `31/90` was claimed as the full ambient value.
   - Current status: **WITHDRAWN**.
   - Correction: `31/90` is the exact value of the full uniform-sign ambient class on `F_3^5`, not of the full ambient problem.

## Superseded

2. **Earlier exchangeable support-`<=2` interpretation.**
   - Earlier statement: the exchangeable support-`<=2` value `E_*` might be the full support-`<=2` optimum.
   - Current status: superseded.
   - Correction: the exchangeable theorem is an exact solved subclass result, not a closure of the full support-`<=2` simplex.

## Corrected closure attempts

3. **Uniform-sign class intermediate overclaims.**
   - Earlier intermediate closure claims before the exact chamber closure are not part of the frozen theorem ledger.

4. **One-distinguished-coordinate closure attempt before corrected elimination / KKT derivation.**
   - Only the corrected exact closure is part of the frozen theorem package.

5. **Computation-assisted checks later replaced by symbolic arguments.**
   - When a symbolic argument supersedes an earlier computation-assisted check, the symbolic argument is the public reference point.

The repository keeps the present ledger sober: corrected claims remain visible, but only current statuses govern the main theorem documents.
